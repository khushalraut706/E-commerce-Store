// All API calls are relative because the frontend is served by the same
// Express server that exposes the API. If you ever split them into two
// separate hosts, just change API_BASE below to the backend's full URL.
const API_BASE = '/api';
